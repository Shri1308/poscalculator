<!DOCTYPE html>
<html>
<head>
    <title>POS MDR Savings Calculator</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f5f5f5;
            padding: 20px;
        }
        .calculator {
            background: white;
            padding: 20px;
            border-radius: 10px;
            width: 350px;
            margin: auto;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }
        h2 {
            text-align: center;
        }
        label {
            display: block;
            margin-top: 10px;
        }
        input, select, button {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            border-radius: 5px;
            border: 1px solid #ccc;
        }
        button {
            background: #4CAF50;
            color: white;
            font-size: 16px;
            cursor: pointer;
            margin-top: 15px;
        }
        button:hover {
            background: #45a049;
        }
        .result {
            margin-top: 20px;
            padding: 10px;
            background: #e8f5e9;
            border-radius: 5px;
            font-weight: bold;
        }
    </style>
</head>
<body>

<div class="calculator">
    <h2>POS MDR Savings Calculator</h2>
    
    <label for="upi">Last Month UPI QR Collection (₹)</label>
    <input type="number" id="upi" placeholder="Enter amount">
    
    <label for="card">This Month Card Swipe Collection (₹)</label>
    <input type="number" id="card" placeholder="Enter amount">
    
    <label for="variant">Account Variant</label>
    <select id="variant">
        <option value="10">₹50K Variant (10%)</option>
        <option value="12">₹1 Lac Variant (12%)</option>
        <option value="15">₹2 Lac Variant (15%)</option>
    </select>
    
    <button onclick="calculateSavings()">Calculate Savings</button>
    
    <div class="result" id="result"></div>
</div>

<script>
function calculateSavings() {
    let upi = parseFloat(document.getElementById("upi").value);
    let card = parseFloat(document.getElementById("card").value);
    let variantPercent = parseFloat(document.getElementById("variant").value);

    if (isNaN(upi) || isNaN(card) || upi <= 0 || card <= 0) {
        document.getElementById("result").innerHTML = "Please enter valid amounts.";
        return;
    }

    // Step 1: Calculate free card transaction limit
    let freeLimit = (upi * variantPercent) / 100;

    // Step 2: Calculate card amount that will be charged
    let chargeableAmount = card - freeLimit;
    if (chargeableAmount < 0) chargeableAmount = 0;

    // Step 3: MDR charges without program
    let mdrWithout = card * 0.015;

    // Step 4: MDR charges with program
    let mdrWith = chargeableAmount * 0.015;

    // Step 5: Savings
    let savings = mdrWithout - mdrWith;

    document.getElementById("result").innerHTML = `
        <p>Free Card Limit: ₹${freeLimit.toFixed(2)}</p>
        <p>MDR Without Program: ₹${mdrWithout.toFixed(2)}</p>
        <p>MDR With Program: ₹${mdrWith.toFixed(2)}</p>
        <p style="color:green;">💰 Savings: ₹${savings.toFixed(2)}</p>
    `;
}
</script>

</body>
</html>
